@model IEnumerable<ImarRuhsatTakipApp.Models.Talep_Tipleri>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Talep_Tip_Ad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Talep_Tip_Ad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Talep_Tip_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Talep_Tip_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Talep_Tip_Id })
        </td>
    </tr>
}

</table>
