@model IEnumerable<ImarRuhsatTakipApp.Models.Durumlar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Durum_Aciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Durum_Aciklama)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.Durum_Id }) |
            @Html.ActionLink("Ayrıntılar", "Details", new { id=item.Durum_Id }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.Durum_Id })
        </td>
    </tr>
}

</table>
